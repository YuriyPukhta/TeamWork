import { Log } from '../loggerSystem';
import { SizeFunction, SizeRange } from '../sizeSystem';
export default function useChangedListContentsSizes(callback: (ranges: SizeRange[]) => void, itemSize: SizeFunction, enabled: boolean, scrollHeightCallback: (height: number) => void, log: Log): (_el: import("./useSize").CallbackRefParam) => void;
